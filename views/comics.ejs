<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>COMICS</title>
</head>
<body>
    <h1>Comics web page</h1>

    <div class="container">
        <% comics.forEach((comic) => { %>
            <div class="card">
                <img class="card-img" src="<%= comic.image %>>" alt="picture of <%=comic.title%>">
                <h3 class="card-title">Title: <%= comic.title %></h3>
                <p class="card-description">Description : <%= comic.description %></p>
            </div>
        <% }) %>
        
    </div>
</body>
</html>